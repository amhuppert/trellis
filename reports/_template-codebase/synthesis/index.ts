import type { SynthesisRoot } from "@trellis/engine";

export const synthesis: SynthesisRoot = {
  description: "Replace this with the codebase's architectural through-line.",
  roots: [
    {
      id: "architecture-shape",
      level: 0,
      title: "Replace with the architecture synthesis",
      summary: "Replace with the common structure across the main modules and flows.",
      keyTakeaways: ["Replace with the most important architecture takeaway."],
      openQuestions: ["Replace with an architecture question worth verifying."],
      references: [{ kind: "section", id: "architecture-overview", anchorId: "overview-map" }, { kind: "source", id: "sample-code" }]
    }
  ]
};
