import type { SynthesisRoot } from "@trellis/engine";

// Empty by default. Add synthesis nodes only when cross-section patterns emerge.
export const synthesis: SynthesisRoot = {
  description: "Replace this when the report has a synthesis layer.",
  roots: []
};
