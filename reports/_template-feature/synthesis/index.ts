import type { SynthesisRoot } from "@trellis/engine";

export const synthesis: SynthesisRoot = {
  description: "Replace this with how user behavior, implementation, and state fit together.",
  roots: [
    {
      id: "feature-synthesis",
      level: 0,
      title: "Feature behavior as an implementation contract",
      summary: "Replace with the central feature contract across UI, backend, and state.",
      keyTakeaways: ["Replace with the main feature takeaway."],
      openQuestions: ["Replace with the highest-risk follow-up question."],
      references: [{ kind: "section", id: "feature-summary", anchorId: "feature-summary-intro" }, { kind: "entity", id: "sample-feature" }]
    }
  ]
};
